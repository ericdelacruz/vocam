@model IEnumerable<SODAMvcApplication.CategoriesServiceReference.Category>
@{
    ViewBag.Title = "Safety On Demand - Categories";
    ViewBag.Description = ""; 
    ViewBag.Keywords = "";
    ViewBag.CurPage = "Category";
    var portalUrl = "http://" + Request.Url.Host.Replace("www","portal")+ "/user/indexpurchase";
}

<div class="categorySection">
    <div class="container">
        <div class="content border">
            <div class="contentHeading border">
                <h1>Browse all of Safety on Demand's Training Categories</h1>
                <p>Safety On Demand Training titles are separated into various training categories. Select a Training category to view all our video training titles which relate to that area of training.</p>
            </div>
            <div class="contentBody border clearfix">
               
                @{int i =1;}
                @while(i <= Model.Count())
                {
                     
                     if( i>1 && (i-1) % 4 == 0 && i <= Model.Count())
                     { 
                        <hr class="hrLine clearfix">  
                     } 
                     
                    <div class="catThumbWrapper clearfix">
                    @for (int j = 0; j < 4 && i <= Model.Count(); j++,i++ )
                    {
                        var cat = Model.ElementAt(i - 1);
                        <div class="catThumb">
                           <a href=@Url.Action("Browse", new { cat = cat.CategoryName.Trim().Replace(" ","-").ToLower()})>
                                  <img alt=@cat.CategoryName src=@HttpUtility.UrlPathEncode(cat.IMG_URL)>
                                       <h2>@cat.CategoryName</h2>
                                                
                                </a>
                        </div>
                    }
                       
                    </div>
                }
              

                <p>&nbsp;</p>
                <p>&nbsp;</p>
                
                @*<a class="purchaseBtn" href="http://portal.sac-iis.com/">Purchase</a>*@
                <a class="purchaseBtn" href="@portalUrl">Purchase</a>
            </div>
        </div>
    </div>

</div>
